# Server
server.port=8080

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://localhost:5432/servicedesk_db
spring.datasource.username=devops_user
spring.datasource.password=SecureDevOps123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.postgresql.Dialect  <-- COMMENTED OUT OR DELETED

# Thymeleaf
spring.thymeleaf.cache=false
